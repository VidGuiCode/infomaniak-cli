[project]
name = "infomaniak-cli"
version = "0.3.0"
description = "A unified CLI for Informaniak/kSuite with profiles and optional future MCP support."
readme = "README.md"
requires-python = ">=3.11"
authors = [{ name = "Gui" }]
license = "MIT"
keywords = ["infomaniak", "ksuite", "cli", "kdrive", "automation"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Environment :: Console",
    "Intended Audience :: End Users/Desktop",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Utilities",
]
dependencies = [
    "keyring>=24.0.0",
]

[project.urls]
Homepage = "https://github.com/VidGuiCode/infomaniak-cli"
Repository = "https://github.com/VidGuiCode/infomaniak-cli"
Issues = "https://github.com/VidGuiCode/infomaniak-cli/issues"
Documentation = "https://github.com/VidGuiCode/infomaniak-cli#readme"

[project.scripts]
ik = "infomaniak_cli.cli:main"

[dependency-groups]
dev = [
    "pytest>=8.0.0",
]

[build-system]
requires = ["uv_build>=0.10.0,<0.12.0"]
build-backend = "uv_build"
